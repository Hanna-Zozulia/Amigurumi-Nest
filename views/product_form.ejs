<%- include('admin/layout_start', { title: (product ? 'Редактирование продукта' : 'Новый продукт'), currentUser, activeSection: 'products' }) %>

<div class="admin-header">
  <h1><i class="bi bi-shop"></i> <%= product ? 'Редактирование' : 'Новый продукт' %></h1>
  <p><%= product ? `Продукт #${product.id}` : 'Добавление в каталог' %></p>
</div>

<div class="card shadow-sm border-0 mb-4">
  <div class="card-body">
    <form method="post" enctype="multipart/form-data" action="<%= product ? ('/products/' + product.id) : '/products' %>">
      <div class="row g-3">
        <div class="col-12 new-product-block">

          <div class="card border-warning bg-warning-subtle shadow-sm">
            <div class="card-body d-flex justify-content-between align-items-center gap-3">

              <div class="form-check m-0">
                <input class="form-check-input border-secondary" type="checkbox" name="isNew" id="isNew"
                  <%= product && product.isNew ? 'checked' : '' %>>
              </div>

              <div class="flex-grow-1">
                <h5 class="mb-1 fw-semibold">✨ Новинка</h5>
                <p class="text-muted mb-0 small">
                  Показывать товар в разделе новинок
                </p>
              </div>

            </div>
          </div>

        </div>
        <div class="col-12">
          <label class="form-label" for="name">Название</label>
          <input class="form-control form-control-lg border border-secondary" id="name" name="name" required value="<%= product ? product.name : '' %>" placeholder="Введите название продукта" />
        </div>

        <div class="col-12 col-md-6">
          <label class="form-label" for="category">Категория</label>
          <select class="form-select form-select-lg border border-secondary" id="categoryId" name="categoryId" required>
            <%# Server-side: render category `<option>` entries for the product form select %>
            <% categories.forEach(category => { %>
              <option value="<%= category.id %>"
                <%= product && product.categoryId === category.id ? 'selected' : '' %>>
                <%= category.name %>
              </option>
            <% }) %>
          </select>
        </div>

        <div class="col-12 col-md-6">
          <label class="form-label" for="price">Цена (EUR)</label>
          <input class="form-control form-control-lg border border-secondary" id="price" name="price" type="number" min="0" step="0.01" required value="<%= product ? product.price : '' %>" placeholder="0.00" />
        </div>

        <div class="col-12">
          <label class="form-label" for="desc">Описание</label>
          <textarea class="form-control border border-secondary" id="desc" name="desc" rows="5" required placeholder="Введите описание продукта"><%= product ? product.desc : '' %></textarea>
        </div>

        <div class="col-12">
          <label class="form-label" for="image">Основное изображение</label>
          <input class="form-control border border-secondary" type="file" id="image" name="image" accept="image/*" required>
          <div class="form-text mt-2">Поддерживаемые форматы: JPEG, PNG, GIF, WebP (максимум 20MB)</div>
          <div class="mt-3">
            <img id="imagePreview" src="<%= product ? normalizeImagePath(product.image) : '' %>" width="100" height="100" class="rounded object-fit-cover border admin-preview-image <%= product && product.image ? 'is-loaded' : '' %>" alt="Preview" />
          </div>
        </div>

        <div class="col-12">
          <label class="form-label" for="image2">Дополнительное изображение</label>
          <input class="form-control border border-secondary" type="file" id="image2" name="image2" accept="image/*">
          <div class="form-text mt-2">Поддерживаемые форматы: JPEG, PNG, GIF, WebP (максимум 20MB)</div>
          <div class="mt-3">
            <img id="imagePreview2" src="<%= product ? normalizeImagePath(product.image2) : '' %>" width="100" height="100" class="rounded object-fit-cover border admin-preview-image <%= product && product.image2 ? 'is-loaded' : '' %>" alt="Preview 2" />
          </div>
        </div>
      </div>

      <div class="mt-4 d-flex gap-2">
        <button class="btn btn-primary btn-lg" type="submit">
          <i class="bi bi-check-circle"></i> <%= product ? 'Обновить' : 'Создать' %>
        </button>
        <a class="btn btn-outline-secondary btn-lg" href="/admin/products">
          <i class="bi bi-x-circle"></i> Отмена
        </a>
      </div>
    </form>
  </div>
</div>

<%- include('admin/layout_end', { activeSection: 'products' }) %>
